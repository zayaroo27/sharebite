package com.sharebite.backend.entity;

public enum NotificationType {
    REQUEST_CREATED,
    REQUEST_APPROVED,
    REQUEST_REJECTED,
    REQUEST_CANCELED,
    REQUEST_COMPLETED,
    NEW_MESSAGE,
    LISTING_EXPIRED
}